apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

metadata:
  name: database

resources:
- deployment.yaml
- service.yaml

commonLabels:
  app: dependency-track
  component: database
  
namespace: dependency-track